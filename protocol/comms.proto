syntax = "proto3";

package comms;

import "google/protobuf/timestamp.proto";

// the packet to send to the controller from the camera.
message Packet {
  // the angle, in trig plane, of the ball.
  float ball_angle = 1;
  // the distance of the ball from the robot.
  float ball_distance = 2;
}
